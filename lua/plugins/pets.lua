return {
--   "giabari/pets.nvim",
--   dependencies = {
--     "edluffy/hologram.nvim", -- Use this instead of holo.nvim
--   },
--   config = function()
-- require("pets").setup({
-- 	  -- default_pet = "dog",   -- Set this to "dog", "cat", "slime", or "crab"
-- 	  -- default_style = "brown", -- Choose the specific color style
-- 	  random = false,        -- Disable random spawning to keep it consistent
-- })
--   end,
}
